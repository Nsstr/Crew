{
  "name": "horarios-app",
  "version": "1.3.0",
  "description": "",
  "main": "import_csv.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node -e \"require('fs').writeFileSync('version.json', JSON.stringify({version: require('./package.json').version}))\"",
    "release": "standard-version"
  },
  "standard-version": {
    "scripts": {
      "postbump": "node update_version.js"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "csv-parse": "^7.0.1",
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
